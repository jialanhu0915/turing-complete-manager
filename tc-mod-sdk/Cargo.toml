[package]
name = "tc-mod-sdk"
version = "0.1.0"
edition = "2021"
description = "Rust SDK for reading/writing Turing Complete circuits and driving the game's compile.dll"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
snap = "1"
libloading = "0.8"
